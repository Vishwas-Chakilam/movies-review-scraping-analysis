# Required Python packages for the Movie Review Scraping and Analysis project

# For web scraping (if you choose to use imdb_scraping.py)
beautifulsoup4==4.12.2
requests==2.31.0

# For data fetching using OMDb API (omdb_api_fetch.py)
requests==2.31.0
pandas==2.1.2

# For data cleaning and analysis
pandas==2.1.2
numpy==1.25.0

# For data visualization
matplotlib==3.8.0
seaborn==0.12.2

# Optional: Power BI integration (use if needed)
# Note: Power BI is not a Python library; it is a separate tool for which you should follow its documentation for integration.

# Add any other dependencies required by your scripts or notebooks
